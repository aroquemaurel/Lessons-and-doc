/* PE : (N > 0) $\wedge$ T(0..N, $\leq$) */
int i  = 1;
int nb = 1;
int t  = 1;

/*  INV  */
while(i != N) {
	/*  INV $\wedge$ C*/
}

/* On remplace une constante par une variable ainsi on a $\neg$C $\wedge$ INV
 * PS : (i = N) $\wedge$ nb $\geq$ 1 $\wedge$ nb = $\nu$I : 0 $\leq$ I $\leq$ i - t 
 * $\wedge$ T[I] = T[I+t-1] $\wedge$ 
 * $\forall$ J : O $\leq$ J $\leq$ i-t-1 $\rightarrow$ T[J] $\neq$ T[J+t] 
 */

/* PS : nb $\geq$ 1 $\wedge$ nb = $\nu$I : 0 $\leq$ I $\leq$ N - t $\wedge$ T[I] = T[I+t-1] $\wedge$ 
 * $\forall$ J : O $\leq$ J $\leq$ N-t-1 $\rightarrow$ T[J] $\neq$ T[J+t] 
 */

