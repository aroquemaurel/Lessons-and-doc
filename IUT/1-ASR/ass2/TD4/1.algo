debut
	empiler(LR, R4);
	R4 <- R0;
	R0 <- (R4 + #ptrcar)$\uparrow$
	appel Free;
	R0 <- #0;
	(R4+#prbCar)$\uparrow$ <- R0;
	R0 <- #NULL;
	(R4+#PtrCar)$\uparrow$ <- R0;
	depiler (R4, LR);
	PC <- LR
fin
	
