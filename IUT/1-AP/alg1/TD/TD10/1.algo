fonction somme (entree <Entier>)
				retourne <Entier>)

glossaire
	s <Entier>;
	i <Entier>;

debut
	s <- 0;
	i <- 1;

	tantque i <= n faire
		s <- s + 1;
		i <- i + 1;
	fin tantque;
	retourner(s);
fin
