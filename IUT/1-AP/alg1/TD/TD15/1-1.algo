-- trier par insertion et rangement dans le tableau tab d'une suite de n valeurs delimitee par un margeur 
-- entraine \forall c \in [i,n-1], tab[i] < tab[i+1]
procedure trierSuiteParInsertion(sortie tab <TabEntier>,
								sortie n <Entier>)
	declanche debordement;

