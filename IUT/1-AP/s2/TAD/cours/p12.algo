glossaire
	l <PtrCellule>;
	nouveau <PtrCellule>;

debut
	si l = NULL alors
		ecrire("La liste est vide!");
	sinon
		allouer(nouveau); --1
		si nouveau = NULL alors
			ecrire("Debordement memoire!");
		sinon
			nouveau$\uparrow$.valeur <- e; --2
			-- chainer la cellule a e2
			nouveau$\uparrow$.suivant<- nouveau;  --3
		fin si;
	fin si;
fin

		
	
