-- SOLUTION DEUX 

procedure lireSuite (sortie suite <TabSuite>)

glossaire
	valeur <Entier>; -- valeur courante
	i <Entier>; -- indice de parcous
	
debut
	i <- 1;
	lire(valeur);
	
	tantque valeur /= 0 faire
		i <- i + 1 ;
		suite.Elem[i] <- valeur;
		lire(valeur);
	fin tantque;
	suite.nbElem <- i - 1;
fin

